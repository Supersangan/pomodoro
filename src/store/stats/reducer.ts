import { ActionCreator, Reducer } from "redux";
import { EModes } from "../../Header/ThemesSwitcher";
import { IStats } from "../reducer";

// // SET MODE
// export const SET_MODE = 'SET_MODE';

// export type TSetTimerMode = {
//   type: typeof SET_MODE;
//   mode: EModes;
// } 

// export const actionSetTimerMode: ActionCreator<TSetTimerMode> = (mode) => ({
//   type: SET_MODE,
//   mode
// });

// export type TStatsActions = TSetTimerMode; 

// export const statsReducer:Reducer<IStats | undefined, TTimerActions> = (state, action) => {
//   return state;
// } 